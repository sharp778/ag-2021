{% import "macros/cards.njk" as cards %}

<div class="max-w-screen-xl  mx-auto py-10 sm:py-12 lg:py-16 ag-container-padding">
  <h1 class="ag-h3 text-gray-50 font-semibold text-left sm:text-center lg:text-left mb-12">Outcomes</h1>

  <div class="grid gap-10 md:gap-16 xl:gap-10 sm:grid-cols-2 xl:grid-cols-3">
    {% for item in solutions.hubs.benefitsAlt %}
      {{ cards.benefitCardBlue(item.icon, item.title, item.description) }}
    {% endfor %}
  </div>
</div>